<div class="container">
  <div class="row">
    <h4 class="title">ジャンル一覧・追加</h4>
  </div>

  <div class="row">
    <div class="col-md-8 my-4">
      <%= form_with model: @genre, url: admin_genres_path, local: true do |f| %>
        <div class="field mb-3">
          <%= f.label :name, "ジャンル名", class: "mr-5 font-weight-bold" %>
          <%= f.text_field :name, autofocus: true, autocomplete: "name", placeholder: "ジャンル名", size: "31x20" %>
          <%= f.submit "新規登録", class: "btn btn-success px-3 pl-4 pr-4 ml-5" %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-md8">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>ジャンル</th>
            <th colspan="2"></th>
          </tr>
        </thead>

        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td style="width: 300px;"><%= genre.name %></td>
              <td><%= link_to "編集する", edit_admin_genre_path(genre), class: "btn btn-success" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>